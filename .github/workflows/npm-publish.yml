name: Publish Package to npmjs
on:
  release:
    types: [published]
jobs:
  Prepare:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.set_version.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '16.x'
          registry-url: 'https://registry.npmjs.org'
          scope: '@wanderword'
      - name: Install dependencies
        run: yarn
      - name: Set version
        id: set_version
        run: |
          TAG_NAME=${GITHUB_REF#refs/tags/}
          echo "Current tag: $TAG_NAME"
          if [[ $TAG_NAME =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            VERSION=${TAG_NAME:1}
          elif [[ $TAG_NAME =~ ^[0-9]+\.[0-9]+$ ]]; then
            VERSION="$TAG_NAME.0"
          else
            echo "Tag name $TAG_NAME does not conform to SemVer (vMAJOR.MINOR.PATCH)"
            exit 1
          fi
          echo "version=$VERSION" >> $GITHUB_ENV
          echo "::set-output name=version::$VERSION"
          npm version $VERSION --no-git-tag-version
      - name: Update .gitignore and commit changes
        run: |
          echo "Setting git config"
          git config --global user.email "dev@wanderword.net"
          git config --global user.name "Automated Release"
          echo "Modifying .gitignore as needed"
          sed -i '/\/dist/d' .gitignore
          echo "/src" >> .gitignore
          echo "/.github" >> .gitignore
          git add .gitignore
          git commit -m "Update .gitignore to exclude src and .github"

  Build:
    needs: Prepare
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '16.x'
          registry-url: 'https://registry.npmjs.org'
          scope: '@wanderword'
      - name: Install dependencies
        run: yarn
      - name: Building package
        run: yarn build
      - name: Create new branch
        run: |
          echo "Setting git config"
          git config --global user.email "dev@wanderword.net"
          git config --global user.name "Automated Release"
          echo "Creating new branch"
          VERSION=${{ needs.Prepare.outputs.version }}
          git switch -c publish/v$VERSION
          git add .
          git commit -m "Publish package version: v$VERSION"
      - name: Generate Changelog
        run: |
          npx auto-changelog --output CHANGELOG.md --tag-prefix '' --template keepachangelog
          git add CHANGELOG.md
          git commit -m "Update CHANGELOG.md"
      - name: Push changes
        run: |
          git push --set-upstream origin "publish/v${{ needs.Prepare.outputs.version }}"

  Publish:
    needs: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '16.x'
          registry-url: 'https://registry.npmjs.org'
          scope: '@wanderword' 
      - name: Install dependencies
        run: yarn
      - name: Run Security Checks
        run: yarn audit
      - name: Dry Run Publish
        run: yarn publish --dry-run
      - name: Publish package
        run: yarn publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
